{
  "name": "intelligent-notification-system",
  "version": "0.1.0",
  "private": true,
  "description": "Event-driven notification pipeline with LLM-based ranking and robust delivery safeguards.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"TODO: add linting\"",
    "test": "echo \"TODO: add tests\"",
    "deploy": "serverless deploy",
    "remove": "serverless remove",
    "local:ranking-demo": "npm run build && node dist/scripts/local-ranking-demo.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.485.0",
    "@aws-sdk/client-s3": "^3.485.0",
    "@aws-sdk/client-sqs": "^3.485.0",
    "@google/generative-ai": "^0.24.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "serverless": "^3.40.0",
    "typescript": "^5.2.2"
  }
}
